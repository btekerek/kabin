"""
Sessions app: the core domain models.

Left empty at scaffold time — Session, Channel, ChannelInterpreter,
Message, RaiseHandEntry, and SessionListener are added together in the
"session CRUD + codes" vertical slice (see project blueprint / ADRs),
once auth exists for them to reference.
"""
